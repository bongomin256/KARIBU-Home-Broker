import React, { createContext, useContext } from "react";

// const PropertyContext = createContext
// const { Provider } = PropertyContext;

export default function GlobalState() {
  return <div>GlobalState</div>;
}
